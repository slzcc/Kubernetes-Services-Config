apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: 
  namespace: default
spec:
  rules:
  - host: data.shileizcc.com
    http:
      paths:
      - path: /
        backend:
          serviceName: kodexplorer
          servicePort: http
